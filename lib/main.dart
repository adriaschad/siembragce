import 'package:flutter/material.dart';

void main() {
  runApp(
    const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Prueba iOS OK', style: TextStyle(fontSize: 24)),
        ),
      ),
    ),
  );
}
